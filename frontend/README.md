## Frontend

Create `.env.local` from `.env.example` and set `NEXT_PUBLIC_API_URL` to your backend API base URL.

Development:

```bash
npm run dev
```

Production:

```bash
npm run build
npm run start
```
